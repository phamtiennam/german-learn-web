import PlaceholderScreen from '../../ui/PlaceholderScreen'

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      subtitle="API keys, voice picker, Drive sync, level."
    />
  )
}
